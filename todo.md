## To do list (8) DONE

1. Make command chaining (`&&`, `||`, `;`) work - required giving built-in commands real exit statuses instead of always "succeeding", so `cd foo && ls` actually means something.

## To do list (7) DONE

1. Make pipes and redirection (`|`, `<`, `>`, `>>`) - works for built-in commands too, not just external ones.

## To do list (6) DONE

1. Make a interactive help command. which sort every command by category (help)
2. kinda smarter command detector?

## To do list (5) DONE

1. Make a command history (up/down)
2. Make a short cut to delete a whole word back. (ctrl + w)
3. Make a short cut to clear a whole line (alt + c)
4. Make a short cut to kill to start-of-line (ctrl + u) / kill to end-of-line (ctrl + k)
5. Make a clear terminal command (clear/clr)

## To do list (4) DONE

1. make a moveable cursor.
2. make a command history. (move to 5)

## To do list (3) DONE

1. Make basic feature :
- blank (make a new blank file)
- mkdir (make directory)
- pcd (print current/working directory)
- rmdir (remove directory)
- rm (remove file)
- mv (move / rename file)
- cp (copy file)
 
## To do list (2) DONE

1. Make a Directory Label.
2. Make a "CD" command.
3. passing the recognizeable command to a normal cmdprmt.
4. make a "touch" command to make a file. (Moved, to list 3)
4. make a "ls" command to list file and folder.

## To do list (1) DONE

1. Make a loop for terminal
2. Make terminal raw mode and disable it if its not use.
3. Make a prompt runner and return anything that user type.
4. Make anything that not registered as a build in command would run a regular cmd command.command 